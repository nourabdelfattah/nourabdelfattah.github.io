_site
.bundle
.sass-cache
.jekyll-cache
.jekyll-metadata
.DS_store
.ruby-version
.tweet-cache
Gemfile.lock
assets/libs/
node_modules/
vendor
.idea

# RStudio
.Rproj.user/
*.Rproj

# Local-only: corporate CA bundle + compose override for Docker builds behind the
# TLS-intercepting network firewall
.ca-bundle.pem
docker-compose.override.yml
vendor/bundle/